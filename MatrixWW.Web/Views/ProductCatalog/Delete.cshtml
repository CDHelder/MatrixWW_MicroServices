<h1>Are you sure you want to delete this?</h1>

<h3>@Model.Name</h3>

<div class="row">
    <table class="table">
        <thead>
        </thead>
        <tbody>
            <tr>
                <td>Price</td>
                <td>@Model.Price</td>
            </tr>
            <tr>
                <td>Category</td>
                <td>@Model.CategoryName</td>
            </tr>
            <tr>
                <td>Brand</td>
                <td>@Model.BrandName</td>
            </tr>
            <tr>
                <td>Description</td>
                <td>@Model.Description</td>
            </tr>
        </tbody>
    </table>
</div>

<form method="post">
    <input type="hidden" value="@Model.Id" />
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-controller="ProductCatalog" asp-action="Index">Back to List</a>
</form>
